{{#each heroes}}
<div class="col-xs-4 hero-info">
        <h2 class = "hero-name">{{name}}</h2>
        <img class = "hero-image" src={{image}}  data-hero-name="{{name}}" />
</div>
{{/each}}

<!-- {{#each villains}}
<div class="villain-info">
        <img src={{image}}  data-villain-name="{{name}}" />
        <h2>{{name}}</h2>
</div>
{{/each}} -->
